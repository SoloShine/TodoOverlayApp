<Window x:Class="TodoOverlayApp.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="待办事项" Height="200" Width="300"
        AllowsTransparency="True" WindowStyle="None" Background="Transparent"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">
    <Border Background="LightBlue" Opacity="0.7" CornerRadius="10">
        <StackPanel Margin="10">
            <!-- 移除这行 -->
            <!-- <TextBlock Text="{Binding TodoContent}" TextWrapping="Wrap"/> -->
            <ListView ItemsSource="{Binding TodoItems}" Height="120">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <CheckBox IsChecked="{Binding IsCompleted}" VerticalAlignment="Center"/>
                            <TextBox Text="{Binding Content}" Width="150" Margin="5,0,0,0"/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="完成" Click="CompleteButton_Click" Margin="5"/>
                <Button Content="编辑" Click="EditButton_Click" Margin="5"/>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
